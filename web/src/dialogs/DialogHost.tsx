/**
 * STUB — host unico de todos os dialogos (intencao de DND, squash, push,
 * remotos, delecao de branch, credenciais do trampolim).
 */
import type { DialogHostProps } from "@/types/modules";

export function DialogHost({ intent }: DialogHostProps) {
  if (!intent) return null;
  return null;
}
