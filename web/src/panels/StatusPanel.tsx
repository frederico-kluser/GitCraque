/** STUB — staging: arquivos alterados, stage/unstage, area de commit. */
import type { PanelProps } from "@/types/modules";

export function StatusPanel({ className }: PanelProps) {
  return <section className={className} data-stub="status" />;
}
