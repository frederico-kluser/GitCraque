/** STUB — console inferior com o argv cru de cada comando git. */
import type { PanelProps } from "@/types/modules";

export function ConsolePanel({ className }: PanelProps) {
  return <section className={className} data-stub="console" />;
}
