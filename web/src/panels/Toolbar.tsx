/** STUB — barra superior: fetch, pull, push, branch, stash, worktree ativa. */
import type { PanelProps } from "@/types/modules";

export function Toolbar({ className }: PanelProps) {
  return <header className={className} data-stub="toolbar" />;
}
