/** STUB — rail esquerdo: worktrees, branches locais, remotos, tags, stashes. */
import type { PanelProps } from "@/types/modules";

export function RailPanels({ className }: PanelProps) {
  return <aside className={className} data-stub="rail" />;
}
