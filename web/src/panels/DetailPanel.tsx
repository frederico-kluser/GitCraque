/** STUB — painel direito: detalhe do commit selecionado e diff. */
import type { PanelProps } from "@/types/modules";

export function DetailPanel({ className }: PanelProps) {
  return <section className={className} data-stub="detail" />;
}
