# sem a barra: numa worktree o node_modules costuma ser um SYMLINK para o do
# repositorio principal, e `node_modules/` (com barra) so casa com diretorio.
node_modules
dist/
.vite/
*.log
npm-debug.log*
.DS_Store
.env
.env.local
coverage/
.gitcraque/
*.tsbuildinfo

# plano de tarefa do project-router: descartavel, apagado no fim de cada tarefa.
# ignorado para que uma tarefa interrompida nao deixe o arquivo commitavel.
TASK_PLAN.md

.deep-orchestrator/

# artefatos do Playwright (web/e2e)
test-results/
playwright-report/
